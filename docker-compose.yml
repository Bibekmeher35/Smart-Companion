services:
  backend:
    build: ./backend
    container_name: smart-companion-backend
    ports:
      - "5050:5050"
    env_file:
      - ./backend/.env

  frontend:
    build: ./frontend
    container_name: smart-companion-frontend
    ports:
      - "3000:80"
    depends_on:
      - backend
